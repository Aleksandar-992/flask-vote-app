
name: CI/CD Workflow

name: CI/CD
 Add CI/CD workflow

on:
  push:
    branches:

      - master  # Проверува дали има промени во master
  pull_request:
    branches:
      - master  # Проверува дали има PR кон master

      - master  # Или вашата главна гранка
  pull_request:
    branches:
      - master  # Или вашата главна гранка
 Add CI/CD workflow

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
          


    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'  # Изберете верзија на Python

 Add CI/CD workflow
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

          
      - name: Run application
        run: |
          python app.py


      - name: Run tests
        run: |
          pytest  # Претпоставува дека користите pytest за тестирање
 Add CI/CD workflow

